<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/styles.css">

    <style>
        .results-body {
            padding-top: 60px;
        }
        .adminr-card {
            background-image: linear-gradient(to right, rgb(13, 110, 253) , white);
            margin: 30px;
            height: 80px;
            padding: 20px 5px 40px 5px
        }
        ul {
            list-style-type: none;
        }
        .list-item {
            display: flex;
            flex-wrap: wrap;
            color: aliceblue;
            max-width: 500px;
            overflow: hidden;
            font-size: 26px;
            margin: 2px 20px 20px 20px;
        }
        .btn-list {
            margin: 0 5px 0 5px;
            float: right;
        }
        
    </style>
</head>
<body>
    <%- include('./partials/signedinnav.ejs') %>
    <div class="results-body">

    </div>
    <%for(let i =0; i< locals.users.length; i++) {%>
        <%if(users[i].role !== 2) {%>
        <div class="card adminr-card">
        
            <div class="card-body">
                <%if(users[i].role === 0) {%>
                <ul style="display:inline-flex; position:relative">
                    <li class="list-item"><%=users[i].email%></li>
                    <li class="list-item"><%=users[i].student.name%></li>
                    <li class="list-item">Student</li>
                </ul>
                <%} else if(users[i].role === 1){%>
                    <ul style="display:inline-flex; position:relative">
                        <li class="list-item"><%=users[i].email%></li>
                        <li class="list-item"><%=users[i].educator.name%></li>
                        <li class="list-item">Educator</li>
                    </ul>
                    <%}%>
                    <a href="/admin/delete/<%=locals.users[i]._id%>"><button type="button" class="btn btn-outline-danger btn-list">Delete</button></a>
                    <%if( locals.users[i].flag === 0 ) {%>
                        <a href="/admin/block/<%=locals.users[i]._id%>"><button type="button" class="btn btn-outline-warning btn-list">Block</button></a>
                    <%} else {%>
                        <a href="/admin/block/<%=locals.users[i]._id%>"><button type="button" class="btn btn-outline-warning btn-list">Unblock</button></a>
                    <%}%>
                    <%}%>
                
                
            </div>
          </div>
    <%}%>
    

</body>
</html>